<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-inherit="point_of_sale.OrderReceipt" t-inherit-mode="extension">
        <!-- Render the whole receipt once per copy (transaction/shop/customer). -->
        <xpath expr="//div[hasclass('pos-receipt')]" position="attributes">
            <attribute name="t-foreach">laundryCopies</attribute>
            <attribute name="t-as">copy</attribute>
            <attribute name="t-key">copy_index</attribute>
        </xpath>

        <!-- Big copy header at the top of each copy (when labelled). -->
        <xpath expr="//ReceiptHeader" position="before">
            <div t-if="copy.label" class="laundry-copy-header" t-esc="copy.label"/>
        </xpath>

        <!-- Tax disclaimer at the very BOTTOM of each copy — appended last, after core's
             company/contact block and its (hidden) "Powered by Odoo" line. Uses the
             copy-header style, so it matches the copy label's size/weight in print too. -->
        <xpath expr="//div[hasclass('pos-receipt')]" position="inside">
            <div class="laundry-copy-header pt-3">THIS IS NOT AN OFFICIAL TAX INVOICE. FOR LAUNDRY CLAIM TRACKING AND INTERNAL REFERENCE PURPOSES ONLY.</div>
        </xpath>

        <!-- Subtotal + VAT lines only for a VAT Registered fiscal position; otherwise
             the receipt goes straight to Total (which core always prints). -->
        <xpath expr="//div[hasclass('pos-receipt-taxes')]" position="attributes">
            <attribute name="t-if">taxTotals and taxTotals.has_tax_groups and laundryShowTaxBreakdown</attribute>
        </xpath>

        <!-- Box the one product line this copy is for (copy + line both in scope). -->
        <xpath expr="//Orderline" position="attributes">
            <attribute name="class">{ 'laundry-boxed': copy.boxedUuid and copy.boxedUuid === line.uuid }</attribute>
        </xpath>
    </t>
</templates>
